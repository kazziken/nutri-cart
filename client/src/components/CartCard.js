import React from 'react'

function CartCard({id, name}) {
  return (
    <div>
        <p>{id}</p>
        {/* <img src={photo}/> */}
        <p>{name}</p>
        <h1></h1>
    </div>
  )
}

export default CartCard;